pipeline {
    agent any
    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Build') {
            steps {
               withMaven(maven: 'Maven 3.9.2') {
                    bat 'mvn clean package'
               }
            }
        }
    }
}
